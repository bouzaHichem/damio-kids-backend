# Firebase Admin Configuration for Push Notifications
# Add these lines to your existing .env file

# Firebase Project ID (already known)
FIREBASE_PROJECT_ID=damiokids-admin

# Option 1: Firebase Service Account Key (RECOMMENDED - More Secure)
# 1. Go to Firebase Console > Project Settings > Service Accounts
# 2. Click "Generate new private key" 
# 3. Download the JSON file
# 4. Copy the ENTIRE JSON content and paste it here as one line:
FIREBASE_SERVICE_ACCOUNT_KEY={"type":"service_account","project_id":"damiokids-admin",...}

# Option 2: Firebase Server Key (ALTERNATIVE - Less Secure)
# 1. Go to Firebase Console > Project Settings > Cloud Messaging 
# 2. Copy the "Server key" and paste it here:
# FIREBASE_SERVER_KEY=your_server_key_here

# Note: Use EITHER Service Account Key OR Server Key, not both
# Service Account Key is more secure and recommended for production